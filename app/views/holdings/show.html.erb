<p>
  <strong>Symbol:</strong>
  <%= @holding.symbol %>
</p>

<p>
  <strong>User:</strong>
  <%= @holding.user_id %>
</p>

<p>
  <strong>Cost per Unit:</strong>
  <%= @holding.cost_per %>
</p>

<p>
  <strong>Amount owned:</strong>
  <%= @holding.amount_owned %>
</p>

<%= link_to 'Edit', edit_holding_path(@holding) %> |
<%= link_to 'Delete Holding', holding_path(@holding), method: :delete, data: { confirm: 'Are you sure?' }%> |
<%= link_to 'Back', holdings_path %> |
